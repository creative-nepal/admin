NEXT_PUBLIC_API_URL=http://localhost:3333
# --- CMS ---------------------------------------------------------------------
# Public site, used to open a draft preview from the content editor
WEB_URL=http://localhost:3000
# Must match creative-nepal-api. Kept server-side only — /api/preview attaches it on redirect.
CONTENT_PREVIEW_SECRET=
